/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['m.media-amazon.com', 'images.pexels.com', 'icon-library.com'],
  },
};

module.exports = nextConfig;
